{
  "name": "tinyapp",
  "version": "1.0.0",
  "description": "Web app using Node that will allow users to shorten long URLs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Estella Song",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.6.1",
    "express": "^4.16.4"
  }
}
